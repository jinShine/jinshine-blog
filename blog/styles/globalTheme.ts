import { extendTheme } from '@chakra-ui/react'

export const globalTheme = extendTheme({
  colors: {
    bg: {
      light: '#f1f5f9',
      dark: 'gray.300',
    },
    point: {
      light: 'white',
      dark: '#2D3748',
    },
  },
})
